<div class="reference-table-container not-content">
  <table class="reference-table">
    <thead class="reference-table-head">
      <tr>
        <th>
          <div>Variable</div>
        </th>
        <th>
          <div>Tailwind Class</div>
        </th>
        <th><div>&nbsp;</div></th>
      </tr>
    </thead>
    <tbody class="reference-table-body"><slot /></tbody>
  </table>
</div>

<style>
  .reference-table-container {
    overflow: auto;
    position: relative;
    width: 100%;

    .reference-table {
      border-collapse: collapse;
      background: transparent;
      display: table;
      text-align: left;
      width: 100%;

      .reference-table-head {
        tr {
          th {
            background: var(--table-header-background);
            font-size: var(--go-font-size-2);
            font-weight: var(--go-font-weight-medium);
            position: sticky;
            top: 0;

            div {
              border-bottom: var(--go-border-width-1) solid;
              border-color: var(--go-color-grey-6);
              height: 100%;
              width: 100%;
            }
          }
        }
      }

      .reference-table-body {
        background: transparent;
        tr {
          background: transparent;
          td {
            background: transparent;
            border-bottom: var(--go-border-width-1) solid;
            border-color: var(--go-color-grey-4);
            border-left: none;
            border-right: none;
            font-size: var(--go-font-size-1);

            code {
              background-color: var(--go-color-accent-3);
              border-radius: var(--go-border-radius-2);
              color: var(--go-color-accent-11);
              font-family: var(--go-font-family-mono);
              padding-inline: var(--go-space-2);
              padding-block: var(--go-space-1);
            }

            &:last-child {
              padding-right: var(--go-space-4);
              padding-block: var(--go-space-3);
            }
          }
        }
      }
    }
  }

  :global([data-theme="light"]) {
    .reference-table {
      --table-header-background: var(--go-color-white);
    }
  }

  :global([data-theme="dark"]) {
    .reference-table {
      --table-header-background: var(--go-color-grey-1);
    }
  }
</style>
