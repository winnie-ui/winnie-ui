---
import Button from "../../../../components/button.astro";
---

<ul class="button-list">
  {
    ["none", "sm", "md", "lg", "round"].map((scale) => {
      return (
        <li class="goho button-list-item">
          <div class="button-list-item-scale">
            <code>{scale}</code>
          </div>
          <div class="goho" data-radius={scale}>
            <Button>Sign up</Button>
          </div>
        </li>
      );
    })
  }
</ul>

<style>
  .button-list {
    border: var(--go-border-width-1) solid var(--go-color-grey-6);
    border-radius: var(--go-border-radius-5);
    list-style: none;
    padding: 0;
    width: 100%;

    .button-list-item {
      border-bottom: var(--go-border-width-2) dashed var(--go-color-grey-6);
      display: grid;
      gap: var(--go-space-7);
      grid-template-columns: 60px 1fr;
      padding-inline: var(--go-space-7);

      &:last-child {
        border-bottom: none;
      }

      .button-list-item-scale {
        align-self: center;
      }

      button {
        max-width: min-content;
      }
    }
  }
</style>
