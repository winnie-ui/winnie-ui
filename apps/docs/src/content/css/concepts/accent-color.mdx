---
title: Accent Color
description: Customize the accent color of your application.
---

import Demo from "~/components/demo/demo.astro";

import { AccentColorExample } from "./_components/accent-color-example.tsx";

Users and companies often have a preference for the accent color of their applications. Winnie CSS supports this using the `data-accent-color` attribute. The accent color attribute modifies the `--wui-color-accent-<value>` css variables which will affect any components that use those variables.

```tsx title="root.tsx" ins="class="winnie-ui"" ins="data-accent-color="purple""
export default function Root() {
  return (
    <html>
      <head></head>
      <body class="winnie-ui" data-accent-color="purple">
        <App />
      </body>
    </html>
  );
}
```

Any color that Winnie CSS supports can be used as an accent color, including [custom colors](http://localhost:4321/css/docs/reference/color#customizing-colors).

## Example

You use the accent color variables to create dynamic color changing experiences for components. Remember, you can use `data-accent-color` on any html element. It doesn't need to just be defined at the top level of your app.

<Demo>
  <AccentColorExample client:only="react" />
</Demo>
