---
import type { Props } from "@astrojs/starlight/props";

const { title, description } = Astro.props.entry.data;
---

<h1 id="_top">{title}</h1>
{description && <p class="page-description">{description}</p>}

<style>
  h1 {
    margin-top: var(--go-space-4);
    font-size: var(--go-font-size-8);
    line-height: var(--go-line-height-8);
    font-weight: var(--go-font-weight-bold);
    color: var(--go-color-grey-12);
  }

  p.page-description {
    color: var(--go-color-grey-10);
    font-size: var(--go-font-size-3);
    font-weight: var(--go-font-weight-normal);
    margin-top: var(--go-space-4);
  }
</style>
